<UserControl x:Class="Flekt.Wpf.App.Views.DocketListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Flekt.Wpf.App.Converters">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold"
                       Foreground="#333" VerticalAlignment="Center"/>
            <Button Content="Refresh" Command="{Binding LoadDocketsCommand}"
                    Padding="12,6" Margin="16,0,0,0" FontSize="13" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Loading indicator -->
        <ProgressBar Grid.Row="1" IsIndeterminate="True" Height="4" Margin="0,0,0,8"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>

        <!-- Docket grid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Dockets}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  AlternatingRowBackground="#F8F8F8" RowHeight="32"
                  ColumnHeaderHeight="36" FontSize="13"
                  BorderThickness="1" BorderBrush="#DDD"
                  SelectionMode="Single">

            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick"
                              Command="{Binding SelectDocketCommand}"
                              CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
            </DataGrid.InputBindings>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Sender" Binding="{Binding Sender}" Width="*"/>
                <DataGridTextColumn Header="Receiver" Binding="{Binding Receiver}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt, StringFormat=g}" Width="150"/>
                <DataGridTextColumn Header="Lines" Binding="{Binding Lines.Count}" Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status bar -->
        <TextBlock Grid.Row="3" Text="{Binding StatusText}" Foreground="#666" FontSize="12" Margin="0,8,0,0"/>
    </Grid>
</UserControl>
