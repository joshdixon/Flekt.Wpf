<UserControl x:Class="Flekt.Wpf.App.Views.InvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Flekt.Wpf.App.Converters">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Invoice Processing" FontSize="20" FontWeight="SemiBold"
                   Foreground="#333" Margin="0,0,0,12"/>

        <!-- Run button + progress -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <Button Content="Run Invoice Process" Command="{Binding RunInvoicesCommand}"
                    Padding="16,10" FontSize="14" HorizontalAlignment="Left"
                    IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBoolConverter}}"
                    Visibility="{Binding IsProcessing, Converter={StaticResource InverseBoolToVis}}"/>

            <StackPanel Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="{Binding StatusText}" FontSize="13" Foreground="#666" Margin="0,0,0,6"/>
                <ProgressBar Value="{Binding ProgressPercent}" Maximum="100" Height="20" Margin="0,0,0,4"/>
                <TextBlock FontSize="12" Foreground="#999">
                    <Run Text="{Binding ProgressPercent, Mode=OneWay}"/><Run Text="%"/>
                </TextBlock>
            </StackPanel>
        </StackPanel>

        <!-- Results grid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding InvoiceGroups}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  AlternatingRowBackground="#F8F8F8" RowHeight="32"
                  ColumnHeaderHeight="36" FontSize="13"
                  BorderThickness="1" BorderBrush="#DDD"
                  Visibility="{Binding HasResults, Converter={StaticResource BoolToVis}}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*"/>
                <DataGridTextColumn Header="Item Count" Binding="{Binding ItemCount}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Total Qty" Binding="{Binding TotalQty}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Total Value" Binding="{Binding TotalValue, StringFormat=C2}" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Avg Unit Price" Binding="{Binding AvgUnitPrice, StringFormat=C2}" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Grand total row -->
        <Border Grid.Row="3" Background="#E8E8E8" Padding="12,8" Margin="0,1,0,0"
                Visibility="{Binding HasResults, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="GRAND TOTAL" FontWeight="Bold" FontSize="13"/>
                <TextBlock Grid.Column="1" Text="{Binding TotalItemCount}" FontWeight="Bold" FontSize="13" HorizontalAlignment="Right"/>
                <TextBlock Grid.Column="2" Text="{Binding TotalQuantity}" FontWeight="Bold" FontSize="13" HorizontalAlignment="Right"/>
                <TextBlock Grid.Column="3" Text="{Binding TotalValue, StringFormat=C2}" FontWeight="Bold" FontSize="13" HorizontalAlignment="Right"/>
                <TextBlock Grid.Column="4" Text="{Binding OverallAvgUnitPrice, StringFormat=C2}" FontWeight="Bold" FontSize="13" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <!-- Status -->
        <TextBlock Grid.Row="4" Text="{Binding StatusText}" Foreground="#666" FontSize="12" Margin="0,8,0,0"
                   Visibility="{Binding HasResults, Converter={StaticResource BoolToVis}}"/>
    </Grid>
</UserControl>
